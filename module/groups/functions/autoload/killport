if [ -z "$1" ]; then
  echo "Usage: killport <port>"
  return 1
fi

local pid=$(lsof -ti:$1)
if [ -n "$pid" ]; then
  kill -9 $pid
  echo "Killed process on port $1 (PID: $pid)"
else
  echo "No process found on port $1"
fi
